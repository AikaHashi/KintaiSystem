<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.repository.KoutsuhiMapper">

	<!-- 登録 -->
	<insert id="insertKoutsuhi"
    parameterType="com.example.domain.kintai.model.Koutsuhi"
    useGeneratedKeys="true" keyProperty="koutsuhiId">
    INSERT INTO t_koutsuhi (
        user_id, date, method, departure, arrival, via, amount, note
    ) VALUES (
        #{userId}, #{date}, #{method}, #{departure}, #{arrival}, #{via},
        #{amount}, #{note}
    )
</insert>

	<!-- 一覧取得（userId指定） -->
	<select id="findByUserId"
		resultType="com.example.domain.kintai.model.Koutsuhi">
		SELECT * FROM t_koutsuhi
		WHERE user_id = #{userId}
		ORDER BY date DESC
	</select>

	<!-- 単体取得 -->
	<select id="findById" resultType="com.example.domain.kintai.model.Koutsuhi">
		SELECT * FROM t_koutsuhi
		WHERE koutsuhi_id = #{koutsuhiId}
	</select>

	<!-- 更新 -->
	<update id="updateOne"
		parameterType="com.example.domain.kintai.model.Koutsuhi">
		UPDATE t_koutsuhi SET
		user_id = #{userId},
		date = #{date},
		method = #{method},
		departure = #{departure},
		arrival = #{arrival},
		via = #{via},
		amount = #{amount},
		note = #{note}
		WHERE koutsuhi_id = #{koutsuhiId}
	</update>

	<!-- 削除 -->
	<delete id="deleteOne">
		DELETE FROM t_koutsuhi
		WHERE koutsuhi_id = #{koutsuhiId}
	</delete>

	<select id="findByUserIdAndYearMonth"
        resultType="com.example.domain.kintai.model.Koutsuhi">
    SELECT * FROM t_koutsuhi
      WHERE user_id = #{userId}
      AND FORMATDATETIME(date, 'yyyy-MM') = #{yearMonth}
    ORDER BY date DESC;
</select>

</mapper>